server:
  port: 8000

spring:
  application:
    name: VIRTUAL_YOUR_ASSET

  datasource:
    url: jdbc:postgresql://localhost:5432/vyra
    username: postgres
    password: qwerty
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: viraasst@gmail.com
    password: rzrg qodc gfmd xspk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %5p [${spring.application.name:},%.-12X{traceId},%.-8X{spanId}] %logger{36} : %msg%n"
  level:
    org:
      hibernate:
        SQL: off

management:
  tracing:
    enabled: true
    sampling:
      probability: 1.0

crypto:
  secret-key: 1234567890123456